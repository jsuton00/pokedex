import React from 'react';

const ErrorMessage = () => {
	return (
		<div className="error-message-container">
			<p className="error-message">Error! No pokemon found.</p>
		</div>
	);
};

export default ErrorMessage;
